#include<stdio.h>
int main()
{
	float a,b,Area;
	printf("Enter a:");
	scanf("%f",&a);
	printf("Enter b:");
	scanf("%f",&b);
	Area=0.5*a*b;
	printf("Area of Triangle is:%f",Area);
	return 0;
}
